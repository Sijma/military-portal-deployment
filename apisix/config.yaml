deployment:
  role: data_plane
  role_data_plane:
    config_provider: yaml

apisix:
  node_listen: 9080
  enable_admin: false

plugins:
  - app-auth
  - openid-connect
  - proxy-rewrite
  - real-ip
  - cors
  - prometheus
